#!/bin/dash

# [[ $# > 0 ]] || echo "usage: legit-init"; exit 0;

if [ $# -ne 0 ]
  then
    echo "usage: legit-init"; 
    exit 0; 
fi

if [ -d ".legit" ]
    then
    echo "$0: error: .legit already exists"; 
    exit 0;
fi

mkdir ".legit";
echo "Initialized empty legit repository in .legit";
